"""REST server for AWETrim trajectory optimization.

Optional subpackage: requires the ``[server]`` extra (FastAPI, uvicorn,
pydantic v2). Nothing here is imported by the core library — import
``awetrim.server.app`` explicitly to build the application.
"""
